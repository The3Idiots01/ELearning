package com.learnova.elearning.module.quiz.entity.enums;

/** Authoring hint used by AI quiz generation; it is not persisted on the quiz. */
public enum QuizDifficulty {
    EASY,
    MEDIUM,
    HARD,
    MIXED
}
